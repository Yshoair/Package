package com.mobiquity.service;

import com.mobiquity.contract.IPackStrategy;
import com.mobiquity.model.Package;

public class BranchAndBoundPackStrategy implements IPackStrategy {
    @Override
    public Package pack(Package p) {
        return null;
    }
}
